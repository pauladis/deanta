[pytest]
# Pytest configuration for Deanta

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = -v --tb=short --strict-markers

# Markers for organizing tests
markers =
    integration: Integration tests (require running endpoint)
    unit: Unit tests (no external dependencies)
